# My Web App

A simple web app built with **React + TypeScript + Vite + Tailwind CSS**.

## 🚀 Setup

1. **Clone the repo**
   ```bash
   git clone https://github.com/your-username/your-repo.git
   cd your-repo

2. Install dependencies

npm install

3. Run in development

npm run dev

4. Preview production build

npm run